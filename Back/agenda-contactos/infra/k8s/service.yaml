apiVersion: v1
kind: Service
metadata:
  name: contacts-service
  labels:
    app: contacts
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 3000
  selector:
    app: contacts-service